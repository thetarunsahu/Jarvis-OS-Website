(function(){
  const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  const details={past:{label:'SAVED DECISION',state:'ARCHIVED / TRACEABLE',why:'Keeps architecture decisions available so future work does not reopen solved questions.',evidence:'Architecture direction + session summary'},now:{label:'ACTIVE RESUME POINT',state:'CURRENT / ACTIONABLE',why:'Compresses the current project state into the smallest useful checkpoint for continuing work.',evidence:'Recent changes + next action + active files'},future:{label:'PROPOSED NEXT STATE',state:'PLANNED / NOT EXECUTED',why:'Separates a suggested next action from work that has actually been completed.',evidence:'Roadmap + unresolved dependency'}};
  function init(){const root=$('.memory');const timeline=$('.memory-timeline',root||document);if(!root||!timeline||$('.memory-inspector',root))return;const events=$$('.memory-event',timeline);const inspector=document.createElement('div');inspector.className='memory-inspector';inspector.innerHTML='<div><small data-memory-label>ACTIVE RESUME POINT</small><strong data-memory-state>CURRENT / ACTIONABLE</strong></div><p data-memory-why></p><span data-memory-evidence></span>';timeline.insertAdjacentElement('afterend',inspector);function activate(el){events.forEach(e=>e.classList.toggle('selected',e===el));const key=el.classList.contains('past')?'past':el.classList.contains('future')?'future':'now',d=details[key];$('[data-memory-label]',inspector).textContent=d.label;$('[data-memory-state]',inspector).textContent=d.state;$('[data-memory-why]',inspector).textContent=d.why;$('[data-memory-evidence]',inspector).textContent=`EVIDENCE / ${d.evidence}`;window.dispatchEvent(new CustomEvent('jarvis:state',{detail:{state:key==='now'?'thinking':key==='future'?'routing':'idle',source:'memory',hold:1100}}));}events.forEach(el=>{el.tabIndex=0;el.setAttribute('role','button');el.setAttribute('aria-label',`Inspect ${$('h3',el)?.textContent||'memory event'}`);el.addEventListener('click',()=>activate(el));el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();activate(el)}})});activate(events.find(e=>e.classList.contains('now'))||events[0]);}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();