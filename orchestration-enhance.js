(function(){
  const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  function init(){const root=$('[data-route-lab]');if(!root)return;const stage=$('.route-lab-stage',root),agent=$('[data-route-agent]',root),tool=$('[data-route-tool]',root);if(!stage||!agent||$('.route-branches',stage))return;const branches=document.createElement('div');branches.className='route-branches';branches.innerHTML=`<div class="branch-cluster agents"><small>SPECIALIST POOL</small>${['Builder','Researcher','Designer','Archivist','Planner'].map(x=>`<span data-branch-agent="${x.toLowerCase()}">${x}</span>`).join('')}</div><div class="branch-cluster tools"><small>TOOL POOL</small>${['Workspace','GitHub','Web','Files','Tasks'].map(x=>`<span data-branch-tool="${x.toLowerCase()}">${x}</span>`).join('')}</div>`;stage.appendChild(branches);function update(){const a=(agent.textContent||'').toLowerCase(),t=(tool.textContent||'').toLowerCase();$$('[data-branch-agent]',branches).forEach(x=>x.classList.toggle('active',a.includes(x.dataset.branchAgent)));$$('[data-branch-tool]',branches).forEach(x=>x.classList.toggle('active',t.includes(x.dataset.branchTool)));root.dataset.routeType=a.includes('research')?'research':a.includes('archiv')?'files':a.includes('planner')?'plan':a.includes('designer')?'design':'build';}new MutationObserver(update).observe(agent,{childList:true,characterData:true,subtree:true});new MutationObserver(update).observe(tool,{childList:true,characterData:true,subtree:true});update();const form=$('[data-route-form]',root);form?.addEventListener('submit',()=>{stage.classList.remove('branch-flash');void stage.offsetWidth;stage.classList.add('branch-flash')});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,800));else setTimeout(init,800);
})();