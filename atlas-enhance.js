(function(){
  const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  const map={0:[6],1:[1,2],2:[2,3],3:[0,4],4:[5],5:[7]};
  function init(){const root=$('#tech-atlas');if(!root)return;const nodes=$$('[data-atlas-node]',root),cards=$$('.atlas-grid article',root),detail=$('.atlas-detail',root);if(!nodes.length||!cards.length)return;const legend=document.createElement('div');legend.className='atlas-focus-legend';legend.innerHTML='<span>CLICK A PIPELINE STEP</span><strong data-atlas-focus>INPUT → Speech Pipeline</strong>';detail.insertAdjacentElement('afterend',legend);function focus(i){cards.forEach((c,n)=>{c.classList.toggle('atlas-linked',(map[i]||[]).includes(n));c.classList.toggle('atlas-dim',!(map[i]||[]).includes(n))});const labels={0:'INPUT → Speech Pipeline',1:'EMBED → Embeddings + Vector Index',2:'MEMORY → Vector Index + Memory Store',3:'ROUTE → Language Model + Agent Runtime',4:'TOOLS → Tool Adapters',5:'VERIFY → Verification Layer'};$('[data-atlas-focus]',legend).textContent=labels[i];window.dispatchEvent(new CustomEvent('jarvis:state',{detail:{state:i<2?'thinking':i===3?'routing':i===4?'executing':'verified',source:'atlas',hold:900}}));}nodes.forEach((n,i)=>n.addEventListener('click',()=>focus(i)));focus(0)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,700));else setTimeout(init,700);
})();